on: [push]

name: AzureLoginSample

jobs:
  build-and-deploy:
    runs-on: windows-latest
    steps:
      - name: Log in with Azure
        uses: azure/login@v1
        with:
          creds: '${{ secrets.AZURE_CREDENTIALS_OIDC_NEW1 }}'
          environment: 'azurecloud'
